<%= form_with(model: @board, local: true) do |form| %>
  <% if @board.errors.any? %>
    <div id="error_explanation">
      <h2><%= @board.errors.count %>件のエラーがあります。</h2>
      <ul>

      <% @board.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :ないよう %>
  </div>
  <div><%= form.text_area :content %>
    <%= form.submit :ほぞん %>
  </div>
<% end %>

<%= link_to "もどる", boards_path %>